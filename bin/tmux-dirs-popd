#!/usr/bin/env bash

CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cli="$CURRENT_DIR/release/cli"
session="$(tmux display -p '#S')"
dir="$("$cli" popd "$session")" || { echo "tmux-dirs-popd failed" >&2; }
[ -d "$dir" ] && echo "$dir" && cd "$dir" || { echo " " >&2; }
